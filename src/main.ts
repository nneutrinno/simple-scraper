import { Result } from "./lib/types";

async function main(): Promise<void> {
    const searchs: string[] = [
        'dia de não sei',
        'foo',
        'bar',
        'xpto',
    ]

    // Paralelizar
    // 
}

function search(text: string): Promise<Result[]> {
    return
}

main()